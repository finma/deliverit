import 'package:flutter/material.dart';

class TelphonePage extends StatelessWidget {
  const TelphonePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Telphone Page',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
